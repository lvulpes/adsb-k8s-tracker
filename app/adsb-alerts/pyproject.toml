[project]
name = "adsb-alerts"
version = "0.1.0"
description = "Sends alerts to discord based on configmap filter rules applied on new aircraft"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncpg>=0.29.0"
]
